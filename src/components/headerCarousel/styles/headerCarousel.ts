import styles from "./headerCarousel.module.css";

export const mainContainerClasses = `${styles.mainContainer} `;

export const separatorImageClasses = `separatorImage position-relative mt-3`;

export const headingClasses = `fs-1 text-white mt-3 primaryFont`;

export const descriptionClasses = `text-white`;

export const buttonClasses = `${styles.buttonStyle} mt-5`;

export const contentContainerClasses = `d-flex position-absolute flex-column align-items-center justify-content-center ${styles.contentContainer}`;

export const contentHeadingClasses = `primaryColor text-uppercase`;
