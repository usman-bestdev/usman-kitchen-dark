import styles from "./orderForm.module.css";

export const mainContainerClasses = `${styles.mainContainer} `;

export const buttonClasses = `${styles.buttonStyle} mt-3`;

export const addressContainerClasses = `${styles.addressContainer}`;

export const formContainerClasses = `${styles.formContainer} text-center`;

export const inputClasses = `${styles.input}`;
